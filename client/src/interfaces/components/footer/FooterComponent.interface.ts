/**
 * @crystal23733
 * @date 24.08.09
 *
 * `FooterLinksProps`는 푸터 링크 컴포넌트에 전달되는 속성을 정의합니다.
 *
 * @interface
 *
 * @property {string} [className] - 추가적인 CSS 클래스 이름 (선택 사항).
 */
export default interface FooterLinksProps {
  className?: string;
}
